# V9.1 manual browser verification

Date: 2026-09-01

## Local setup

- Backend: `backend/.venv/Scripts/python.exe -m uvicorn app.main:app --host 127.0.0.1 --port 8000`.
- Frontend: `npm run dev -- --host 127.0.0.1 --port 5173`.
- Browser opened `http://127.0.0.1:5173/`.

## With API-key configuration

- Uploaded the representative local PDF `backend/data/uploads/1fed89d6bd06/source.pdf`
  (2.0 MB) through the upload control in BALANCED mode.
- The document completed with `[READY]`, `25 NODES`, and `63 EDGES`; the registry
  displayed parse/chunk/statistical/LLM/total timings and selected-chunk counts.
- Opened the graph view. It exposed node search, path FROM/TO selectors and TRACE,
  provenance filters (`LLM EXTRACTED`, `CO-OCCURRENCE`, `LLM + CO-OCCURRENCE`),
  direction filters (`DIRECTED ASSERTION`, `UNDIRECTED ASSOCIATION`), confidence
  controls, communities, and the 25-concept/63-link network summary.
- Exercised the path selectors/TRACE control and clicked `EXPORT .HTML`, `REPORT
  .MD`, and `CSV .ZIP`. No browser console messages or page errors were reported.

## Without API-key configuration

- Restarted the backend with `OPENAI_API_KEY=` and confirmed `/api/config` returned
  `"has_api_key": false`.
- The UI displayed `LLM LINK OFFLINE · STAT MODE` and the no-key banner explaining
  keyword co-occurrence fallback.
- Uploaded the same local PDF in BALANCED mode; it completed `[READY]` with
  `25 NODES` and `63 EDGES`.

This is a local smoke check of the documented flow; it does not claim semantic
quality for provider-generated relations.
